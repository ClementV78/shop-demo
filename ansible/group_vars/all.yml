---
project_name: shop-demo
project_root: "{{ playbook_dir | dirname }}"
local_kubernetes_distribution: k3s
use_local_ministack: true
local_host_user: "{{ lookup('env', 'SUDO_USER') | default(lookup('env', 'USER'), true) }}"
