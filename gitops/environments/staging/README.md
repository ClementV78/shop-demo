# Staging Environment

Cet environnement sert a valider le flux GitOps local avant promotion.

Etat `S1-T1` :

- namespace `shopdemo-staging` ;
- aucun workload applicatif ;
- aucune dependance a une API externe.

