---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Cet assemblage ne porte plus que le SOCLE de l'environnement : le namespace
# et ses policies. Les applications qui tournent dedans sont generees une par
# une par gitops/argocd/applicationset-staging.yaml.
resources:
  - namespace.yaml
  - networkpolicies.yaml
