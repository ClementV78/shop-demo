# Argo CD

Ce repertoire accueillera les ressources Argo CD du Sprint 1.

Elements prevus :

- installation Argo CD ou reference Helm/Kustomize ;
- `AppProject` pour borner le perimetre ShopDemo ;
- `ApplicationSet` staging ;
- `ApplicationSet` prod base sur tags ;
- sync waves pour ordonner les ressources.

`S1-T1` ne cree pas encore ces objets. Le but est de cadrer la structure avant
l'installation reelle d'Argo CD.

